TODO stuffs for Ocsinventory::Agent

minor:
 * translate the software
 * in daemon mode, store the time remaining before the upload
 * call md5sum directly if avalaible
 * prepare the package to be CPAN ready
 * force an upload if change append on the host

important:
 * being able to set http_proxy
 * being able to set the network port
 * use nmap if ipdiscover is not avalaible
 * the current XML format doesn't allow us to write correctly the
   IP routes
