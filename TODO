TODO stuffs for Ocsinventory::Agent

 * translate the software
 * find an alternative to dmidecode for non-PC plateforme
 * in daemon mode, store the time remaining before the upload
 * If Compress::Zlib is not avalaible, calls gzip instead to
 compress the XML
